export const plugins = {
    // Exporting some plugins here, because the CSS party isn't complete without them.
    
    tailwindcss: {},
    // TailwindCSS plugin—because who wants to write boring, old-school CSS from scratch? 
    // This guy makes sure all the fancy Tailwind classes actually work in your project.
  
    autoprefixer: {},
    // Autoprefixer, aka the little helper that makes sure your CSS works across all browsers. 
    // It’s like the translator at the UN for different browsers: 
    // "Oh, you need a -webkit- prefix? Here you go. Oh, you're Firefox? Gotcha, we'll fix that for you, I personally like Chorme though :)"
  };
  
  
  